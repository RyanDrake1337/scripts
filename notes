# Jupyter + Pandas + SQLite Environment Setup
```Start a local virtual enviroment with venv python module```

python3 -m venv ~/jupyter_env
source ~/jupyter_env/bin/activate
jupyter notebook &

```install required packages if needed```
pip install --upgrade pip
pip install notebook pandas sqlite3-binary

```Example code to use Jupyter notebook to import required libraries```
import pandas as pd
import sqlite3

```Git push example```
  178  git add .
  179  git commit -m "notes here"
  181  git push -u origin main



conn = sqlite3.connect(":memory:") #this creates a variable to use the sqlite3 connect module to connect to a db
result = pd.read_sql_query("SELECT * FROM users;", conn)
print(result)
